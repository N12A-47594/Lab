#include <iostream>
#include <cstdlib>
#include <time.h>
#include <fstream>

using namespace std;

int *T;
int m;
int n;
int Suma, Srednia;
fstream Tablica;

int menu(int m, int n)				//Ojciec
{
	switch(m)
	{
		cout<<"Co chcialbys zrobic z tablica?"<<endl;
		cout<<"1. Wypelnic losowymi zmiennymi."<<endl;
		cout<<"2. Posortowac."<<endl;
		cout<<"3. Policzyc srednia wartosc zmiennych."<<endl;
		cout<<"4. Podejrzec najnizsza wartosc."<<endl;
		cout<<"5. Podejrzec najwyzsza wartosc."<<endl;
		cout<<"6. Obejrzec Tablice."<<endl;
		cout<<"7. Zapisac Tablice."<<endl;
		cout<<"8. Wczytac Tablice."<<endl;
		case 1:
			generateArray(n);
		case 2:
			sortArray(n);
		case 3:
			averageValue(n);
		case 4:
			minimumValue();
		case 5:
			maximumValue();
		case 6:
			printArray();
		case 7:
			saveArray(*T);
		case 8:
			loadArray(*T);
		default:
			cout<<"Nie kombinuj."<<endl;
	}
}

int saveArray(*T)				//Pierworodny
{
	Tablica.open("Tablica.txt", ios::out);
	Tablica<<*T<<endl;
	Tablica.close();
}

int  loadArray(*T)
{
	Tablica.open("Tablica.txt", ios::in);
	if(Tablica.good()==false)
	{
		cout<<"Cos zgubilismy."<<endl;
	}
	string nitka;
	getline(Tablica,nitka);
}

int generateArray(int n)		//Matka
 {
	srand(time(NULL));
	for(int i=0; i<n; i++)
	{
		T[i] = rand()%100;
	}
	cout<<endl;
 }

int sortArray(int n)			//Pierworodna
{
	for (int j=1; j<n; j++)
	{
		for (int i=n; i>=1; i--)
		{
			if (T[i]<T[i--])
			{
				int reka;
				reka = T[i--];
				T[i--] = T[i];
				T[i] = reka;
			}
		}
	}
}

int averageValue(int n) 			//Malcolm
{
	for (int i=0;i<n;i++)
	{
		Suma += T[i];
	}
	
	Srednia = Suma/n;
	cout<<"Srednia: "<<Srednia<<endl;
}

int maximumValue()					//Starsza Bliźniaczka
{
	for (int j=1; j<n; j++)
	{
		for (int i=n; i>=1; i--)
		{
			if (T[i]<T[i--])
			{
				int reka;
				reka = T[i--];
				T[i--] = T[i];
				T[i] = reka;
			}
		}
	}
	cout<<"Najwieksza: "<<T[n]<<endl;
}

int minimumValue() 					//Młodsza Bliźniaczka
{
	for (int j=1; j<n; j++)
	{
		for (int i=n; i>=1; i--)
		{
			if (T[i]<T[i--])
			{
				int reka;
				reka = T[i--];
				T[i--] = T[i];
				T[i] = reka;
			}
		}
	}
	cout<<"Najmniejsza: "<<T[0]<<endl;
}

int printArray() 					//Bobo
{
	cout<<"Tablica:"<<endl;
	for(int i=0; i<n; i++)
	{
		cout<<T[i]<<", ";
	}
	cout<<endl;	
}	

int main()
{
	
	cout<<"Stworzmy Tablice!"<<endl;
	cout<<"Podaj rozmiar Tablicy:"<<endl;
	cin>>n;
	T = new int [n];
	
	menu();
	

	
	delete [] T;
	return 0;
}
